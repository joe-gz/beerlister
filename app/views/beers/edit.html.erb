<div class = "wrapper">
  <h2 class = "edit_beer">Edit <%=@beer.name%> </h2>
  <!-- standard form_for to edit the review on a given beer
  Only the user who posted the beer can edit -->
  <%= form_for @beer do |f| %>

  <div>
    <%= f.label :review %>
  </div>
  <%= f.text_area :review, size: "18x4" %>

  <%= f.submit class:"submit"%>

  <%end%>
</div>
